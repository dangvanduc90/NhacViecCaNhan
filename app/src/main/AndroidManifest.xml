<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chung.nhacvieccanhan">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoaiCongViecActivity"
            android:label="@string/title_activity_loai_cong_viec"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CongViecActivity"
            android:label="@string/title_activity_cong_viec"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ThoiGianBaoThucActivity"
            android:label="@string/title_activity_thoi_gian_bao_thuc"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ThoiGianLapActivity"
            android:label="@string/title_activity_thoi_gian_lap"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ThemLoaiCongViecActivity" />
        <activity android:name=".SuaLoaiCongViecActivity" />
        <activity android:name=".ChiTietLoaiCongViecActivity" />
        <activity android:name=".ThemCongViecActivity" />
        <activity android:name=".SuaCongViecActivity" />
        <activity android:name=".ChiTietCongViecActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MapDirectionActivity" />

        <receiver android:name=".broadcast.AlarmBroadCast">
            <intent-filter>
                <action android:name="YES_ACTION" />
                <action android:name="STOP_ACTION" />
            </intent-filter>
        </receiver>

        <service
            android:name=".model.SongService"
            android:enabled="true" />
        <service
            android:name=".model.TestService"
            android:enabled="true" />

        <receiver android:name=".model.AlarmReceiver" />

        <activity android:name=".AlarmScreenActivity" />
        <activity android:name=".ThemThoiGianLapActivity"></activity>
    </application>

</manifest>